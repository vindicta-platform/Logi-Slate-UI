# Logi-Slate-UI Pre-Commit Configuration
# Profile: frontend-docs
# Phase 1: Lockdown — hygiene, markdown lint, commitizen

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
      - id: detect-private-key

  - repo: https://github.com/commitizen-tools/commitizen
    rev: v4.4.1
    hooks:
      - id: commitizen
        stages: [commit-msg]

  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.17.2
    hooks:
      - id: markdownlint-cli2
        args: []

  - repo: local
    hooks:
      - id: wip-check
        name: "block WIP commits"
        entry: bash -c 'if echo "$1" | head -1 | grep -iqE "^wip[^a-z]|^wip$"; then echo "ERROR: WIP commits not allowed"; exit 1; fi'
        language: system
        stages: [commit-msg]
        always_run: true
